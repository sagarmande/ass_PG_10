Here are Python programs to accomplish the tasks you've mentioned:

1. Program to find the sum of elements in a list:

```python
def sum_of_elements(lst):
    return sum(lst)

# Example usage:
my_list = [1, 2, 3, 4, 5]
result = sum_of_elements(my_list)
print("Sum of elements:", result)
```

2. Program to multiply all numbers in a list:

```python
def multiply_elements(lst):
    result = 1
    for num in lst:
        result *= num
    return result

# Example usage:
my_list = [1, 2, 3, 4, 5]
result = multiply_elements(my_list)
print("Product of elements:", result)
```

3. Program to find the smallest number in a list:

```python
def find_smallest(lst):
    return min(lst)

# Example usage:
my_list = [5, 2, 8, 1, 9]
result = find_smallest(my_list)
print("Smallest number:", result)
```

4. Program to find the largest number in a list:

```python
def find_largest(lst):
    return max(lst)

# Example usage:
my_list = [5, 2, 8, 1, 9]
result = find_largest(my_list)
print("Largest number:", result)
```

5. Program to find the second largest number in a list:

```python
def find_second_largest(lst):
    sorted_lst = sorted(lst, reverse=True)
    return sorted_lst[1]

# Example usage:
my_list = [5, 2, 8, 1, 9]
result = find_second_largest(my_list)
print("Second largest number:", result)
```

6. Program to find the N largest elements from a list:

```python
def find_n_largest(lst, n):
    sorted_lst = sorted(lst, reverse=True)
    return sorted_lst[:n]

# Example usage:
my_list = [5, 2, 8, 1, 9, 7, 6]
n = 3
result = find_n_largest(my_list, n)
print(f"{n} largest elements:", result)
```

7. Program to print even numbers in a list:

```python
def print_even_numbers(lst):
    even_numbers = [num for num in lst if num % 2 == 0]
    print("Even numbers:", even_numbers)

# Example usage:
my_list = [1, 2, 3, 4, 5, 6]
print_even_numbers(my_list)
```

8. Program to print odd numbers in a list:

```python
def print_odd_numbers(lst):
    odd_numbers = [num for num in lst if num % 2 != 0]
    print("Odd numbers:", odd_numbers)

# Example usage:
my_list = [1, 2, 3, 4, 5, 6]
print_odd_numbers(my_list)
```

9. Program to remove empty lists from a list of lists:

```python
def remove_empty_lists(lst):
    return [sublist for sublist in lst if sublist]

# Example usage:
my_list = [[], [1, 2], [], [3, 4], []]
result = remove_empty_lists(my_list)
print("List after removing empty lists:", result)
```

10. Program to clone or copy a list:

```python
def clone_list(lst):
    return lst.copy()

# Example usage:
original_list = [1, 2, 3, 4, 5]
copied_list = clone_list(original_list)
print("Original list:", original_list)
print("Copied list:", copied_list)
```

11. Program to count occurrences of an element in a list:

```python
def count_occurrences(lst, element):
    return lst.count(element)

# Example usage:
my_list = [1, 2, 2, 3, 2, 4, 5]
element_to_count = 2
count = count_occurrences(my_list, element_to_count)
print(f"Occurrences of {element_to_count}: {count}")
```

You can use these programs as templates and adapt them to your specific requirements.
